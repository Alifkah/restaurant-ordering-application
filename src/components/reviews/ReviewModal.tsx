"use client";

import PhotoReviewModal, { PhotoReviewModalProps } from "./PhotoReviewModal";

export default function ReviewModal(props: PhotoReviewModalProps) {
  return <PhotoReviewModal {...props} />;
}
